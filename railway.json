{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "DOCKERFILE",
      "buildCommand": "docker build -t frontend ./frontend && docker build -t backend ./backend",
      "nixpacksPlan": {
        "providers": ["node"]
      },
      "watchPatterns": [
        "frontend/*",
        "backend/*"
      ]
    },
    "deploy": {
      "runtime": "V2",
      "numReplicas": 1,
      "startCommand": "docker run -d -p 3306:3306 --name mysql -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=app_db mysql:8.0.41 && docker run -d -p 80:80 frontend && docker run -d -p 8000:8000 backend",
      "healthcheckPath": "/api/health",
      "sleepApplication": false,
      "multiRegionConfig": {
        "us-west2": {
          "numReplicas": 1
        }
      },
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }